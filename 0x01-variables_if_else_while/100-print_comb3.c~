#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
  int i = 0, i1;
  while (i <= 9)
    {
      i2 = 0;
      while (i2 <= 9)
	{
	  if (i !=  i2 &&i < i2)
	    {
	      putchar(i + 48);
	      putchar(i2 + 48);
	      if (i + i2 != 17)
		{
		  putchar(',');
		  putchar(' ');
		}
	    }
	  ++i2;
	}
      ++i;
    }
  putchar ('\n');

  return (0);
}
