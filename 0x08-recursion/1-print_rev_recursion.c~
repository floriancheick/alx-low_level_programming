#include "main.h"
#include <stdio.h>

/**
*_print_rev_recursion - This function prints a  string reverse.
*
*@s: Pointer to use
*
*Return: Void
*/

void _print_rev_recursion(char *s)
{
if (*s == '\0')
return;

_print_rev_recursion(s + 1);
_putchar(*s);
}
